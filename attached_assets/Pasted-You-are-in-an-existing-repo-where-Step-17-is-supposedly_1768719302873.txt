You are in an existing repo where Step 17 is supposedly complete (Vercel deploy support + production gates).
DO NOT modify any files unless something is missing or incorrect.
Your job is to VERIFY, then report with evidence.

GOAL
Confirm Step 17 is fully implemented and production-safe by:
1) verifying required files exist
2) verifying file contents match the Step 17 spec
3) verifying package.json scripts + devDependencies
4) running QA gates and pasting full output
5) returning a GO/NO-GO verdict with reasons

NON-NEGOTIABLE RULES
- Do NOT claim tests passed unless you actually ran them.
- Do NOT rewrite files “for cleanliness.”
- If anything differs, show the exact diff needed (minimal patch only).

STEP 1 — VERIFY FILES EXIST
Run:
ls -la vercel.json
ls -la scripts/db-migrate-and-verify.ts
ls -la docs/DEPLOYMENT.md

STEP 2 — PRINT FILE CONTENTS (EVIDENCE)
Print the full contents:
cat vercel.json
sed -n '1,220p' scripts/db-migrate-and-verify.ts
sed -n '1,260p' docs/DEPLOYMENT.md

Validation checks:
A) vercel.json must include:
- buildCommand: "pnpm db:generate && pnpm build"
- installCommand: "pnpm install"
- framework: "nextjs"
- regions includes "iad1"

B) scripts/db-migrate-and-verify.ts must:
- call pnpm db:migrate (NOT npx prisma migrate deploy directly)
- verify DB connectivity schema-agnostically with SELECT 1 (prisma.$queryRaw`SELECT 1`)
- fail clearly if DATABASE_URL missing

C) docs/DEPLOYMENT.md must:
- render cleanly on GitHub (all code fences closed)
- explicitly state Vercel does NOT automatically run migrations
- include manual migration steps
- include env vars tables
- include smoke test curl examples

STEP 3 — VERIFY package.json KEYS (EVIDENCE)
Run:
cat package.json

Confirm these scripts exist EXACTLY (values must match):
- db:generate: "prisma generate"
- db:migrate: "prisma migrate deploy"
- db:migrate:dev: "prisma migrate dev"
- db:studio: "prisma studio"
- db:push: "prisma db push"
- db:seed: "tsx prisma/seed.ts"
- db:migrate-verify: "tsx scripts/db-migrate-and-verify.ts"
- deploy:check: "pnpm lint && pnpm typecheck && pnpm test:e2e && pnpm build"
- postinstall: "prisma generate"
- test:e2e: "playwright test" (if missing, note it)

Confirm devDependencies includes:
- tsx (any stable version is fine)

STEP 4 — RUN QA GATES (PASTE FULL OUTPUT)
Run in this exact order:
pnpm lint
pnpm typecheck
pnpm test:e2e
pnpm db:generate
pnpm build
pnpm db:migrate-verify

If db:migrate-verify fails due to missing DATABASE_URL:
- STOP and report exactly how to set Replit Secret:
  Name: DATABASE_URL
  Value format: postgresql://user:pass@host/db?sslmode=require

STEP 5 — OUTPUT FORMAT (MUST FOLLOW)
Return in this exact structure:

A) File existence check results (ls outputs)
B) vercel.json contents
C) scripts/db-migrate-and-verify.ts contents (first 220 lines)
D) docs/DEPLOYMENT.md contents (first 260 lines)
E) package.json relevant sections (scripts + devDependencies)
F) QA gate outputs (full logs)
G) Verdict:
- GO if all checks match + all gates pass
- NO-GO if any mismatch or any gate fails (include minimal diff patch required)