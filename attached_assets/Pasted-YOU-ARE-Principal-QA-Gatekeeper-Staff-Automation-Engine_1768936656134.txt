YOU ARE: Principal QA Gatekeeper + Staff Automation Engineer + Security Tester.
MISSION: Create an A-to-Z test suite that verifies every user-visible and business-critical function in the Treasure Coast AI platform.
This includes EVERY clickable object (links/buttons/icons/logo), EVERY text field, EVERY form, EVERY workflow, and all RBAC + tenant isolation constraints.
This must be production-grade, stable, and repeatable.

NON-NEGOTIABLES
1) NO FLAKY TESTS. If any test is flaky, fix it by adding deterministic selectors (data-testid) and waiting for stable states.
2) EVERYTHING MUST BE ASSERTED: not just click — verify result (URL change, toast, API response, UI update, DB persistence when relevant).
3) FULL COVERAGE: marketing site + app dashboard + settings + widget + public chat + booking funnel + analytics + lead inbox + notifications + export.
4) RBAC + TENANT ISOLATION: verify CLIENT restrictions, allowClientEdits gates, OWNER/ADMIN privileges, and cross-tenant blocking.
5) “TINY THINGS COUNT”: clicking logo returns home, keyboard submit works, disabled buttons stay disabled, form errors render, empty states show correctly.
6) OUTPUT: a QA Evidence Report at the end with:
   - files changed
   - tests added counts
   - command outputs:
     pnpm typecheck
     pnpm test
     pnpm test:e2e
     pnpm build

ASSUMED FEATURE SET (ALREADY IMPLEMENTED)
- Multi-tenant Organizations and RBAC via Clerk
- Org settings: Services + Hours + Branding + Custom Domain + Notifications
- Truth Mode chat with grounded answers only
- Deterministic booking flow FSM + widget UI directives
- Analytics dashboard with funnel metrics and unique conversation counting
- Lead inbox + lead management + CSV export
- Setup progress tracking
- Hot lead notifications via Resend

YOUR JOB (HIGH LEVEL)
A) Build a “Test Map” (route inventory + interactive elements inventory)
B) Add data-testid attributes across the app so Playwright can click everything reliably
C) Implement Playwright E2E tests for:
   - NAVIGATION COVERAGE (every route)
   - UI INTERACTION COVERAGE (every clickable object on those routes)
   - FORMS COVERAGE (every form field + validation + submission + persistence)
   - PERMISSION MATRIX (OWNER vs ADMIN vs CLIENT allowClientEdits=false vs CLIENT allowClientEdits=true)
   - TENANT ISOLATION (Org A cannot see Org B data anywhere)
   - WIDGET + PUBLIC CHAT (truth mode + booking flow + click tracking)
   - ANALYTICS + LEADS (data created in flows appears in dashboards)
   - NOTIFICATIONS (mock Resend; assert NotificationLog writes and “would send” behavior)
   - EXPORTS (CSV download is correct)
   - SMALL UX DETAILS (logo click to home, footer links, 404, loading skeletons, toasts)

D) Add Unit Tests where needed for helpers/utilities that become test-critical.

TEST INFRA REQUIREMENTS
1) Add/confirm a stable Playwright config:
   - baseURL from env (e.g. PLAYWRIGHT_BASE_URL or NEXT_PUBLIC_APP_URL)
   - retries=0 for stable tests (increase only if truly needed)
   - trace/video/screenshots on failure
2) Add a seed script or test helper that creates:
   - Org A + Org B
   - Bots for each org with distinct publicKey
   - Services + Hours for Org A
   - Owner/Admin/Client users in Clerk OR a test auth bypass ONLY IN TEST MODE
   - Conversations/leads created via APIs where required

AUTH STRATEGY (IMPORTANT)
- Do NOT rely on manual login in tests.
- Implement a test-only auth mechanism:
  Option 1 (preferred): a Playwright “storageState” generated via Clerk test tokens.
  Option 2: a test-only server helper that accepts x-test-user-id header ONLY when NODE_ENV=test.
- Under NO circumstance should test bypass work in production builds.
- Add explicit unit test verifying bypass disabled in production.

DATA-TESTID STRATEGY (MANDATORY)
- Every interactive element must have data-testid.
- Add a lightweight convention:
  - data-testid="nav-logo"
  - data-testid="nav-home"
  - data-testid="settings-services-link"
  - data-testid="services-add-button"
  - data-testid="services-modal"
  - data-testid="services-name-input"
  - data-testid="services-bookingUrl-input"
  - data-testid="hours-save-button"
  - data-testid="widget-service-button-<serviceId>"
  - data-testid="booking-link-button"
  - data-testid="leads-row-<leadPublicId>"
  - etc.
- Add these to the UI components and ensure no duplicates on page.

===========================================================
PHASE 1 — INVENTORY: ROUTES + INTERACTIONS (AUTOGENERATED)
===========================================================
STEP 1) Create a “Route Inventory” doc generated from the running Next.js app:
- Include public routes: "/", "/pricing", "/demo", "/request-demo", any legal pages, etc.
- Include app routes: "/app", "/app/settings", "/app/settings/services", "/app/settings/hours", "/app/settings/branding", "/app/settings/custom-domain", "/app/analytics", "/app/leads", "/app/bots", etc.
- Include widget routes: "/widget/[botPublicKey]" (and any embed variants).
- Include API routes inventory in a dev-only script that lists src/app/api/**/route.ts.

Deliverable:
- docs/QA_ROUTE_INVENTORY.md
- docs/QA_INTERACTION_INVENTORY.md (list of clickables/forms per route)

STEP 2) Create a Playwright “UI Crawler” (SAFE CLICKER) that:
- visits each route in the inventory
- collects all elements matching:
  a[href], button, [role=button], input, textarea, select, [data-testid]
- logs them into docs/QA_INTERACTION_INVENTORY.md
- attempts “safe clicks” ONLY on elements with data-testid starting with:
  - "nav-" "cta-" "link-" "btn-" "action-" "open-" "modal-"
(so it doesn’t accidentally delete data or log out)
- this crawler is for coverage auditing, not for pass/fail.

Files:
- tests/e2e/_crawler/uiInventory.spec.ts
- scripts/generateRouteInventory.ts

===========================================================
PHASE 2 — CORE SMOKE TESTS (EVERY PAGE LOADS)
===========================================================
Create tests/e2e/smoke.spec.ts:
- For each major route, ensure:
  - page loads (no 500)
  - main header/title is visible
  - left nav visible in app routes
  - no console errors (fail on pageerror + severe console errors)
  - basic accessibility scan (at least: no missing <title>, no duplicate IDs)

===========================================================
PHASE 3 — NAVIGATION + “CLICK EVERYTHING”
===========================================================
Create tests/e2e/navigation.spec.ts:
PUBLIC NAV
- Click logo => returns to "/"
- Header nav links:
  - Home
  - Pricing
  - Demo
  - Request Demo
- Footer links (if present)
- Each click must assert:
  - URL change is correct
  - page heading contains expected text
  - the active nav state highlights appropriately (if implemented)

APP NAV
- In authenticated app:
  - click each nav item: Dashboard, Bots, Leads, Analytics, Settings
  - click Settings hub cards: Services, Hours, Branding, Custom Domain, Notifications
- Each click must assert:
  - URL is correct
  - page title visible
  - skeleton resolves to content
  - no infinite loading state

===========================================================
PHASE 4 — FORMS: VALIDATION + SUBMIT + PERSISTENCE
===========================================================

A) SERVICES SETTINGS — CRUD + ORDER + DUPLICATE + URL NORMALIZATION
tests/e2e/services.spec.ts:
Run for roles:
  1) OWNER (allowClientEdits=false)
  2) CLIENT (allowClientEdits=false)
  3) CLIENT (allowClientEdits=true)

Assertions:
OWNER:
- Open services page => list renders
- Add service modal opens
- Field validation:
  - empty name => inline error
  - invalid bookingUrl => inline error
  - price conversions stable (if used)
- Create service => toast success + service appears in list
- Edit service => updates persist after reload
- Reorder service up/down => order persists after reload
- Duplicate service name (case-insensitive) => 409 handled, UI shows error
- Delete service => confirm modal => removed from list

CLIENT allowClientEdits=false:
- page renders but shows locked banner
- Add/Edit/Delete controls disabled or hidden
- attempts should not work; ensure UI prevents and API returns 403 if forced

CLIENT allowClientEdits=true:
- same as OWNER for allowed actions (or subset if intended)

B) HOURS SETTINGS — 7 DAY GRID + VALIDATION + SAVE + REVERT
tests/e2e/hours.spec.ts:
For roles same matrix as above.
Assertions:
- 7 day rows visible (Sun-Sat)
- Toggle closed day => times disabled/null
- Invalid time ordering (open > close) shows error and Save disabled
- Save valid schedule => toast success
- Reload => persisted
- Dirty state triggers “Revert Changes” => returns to saved state
- Locked state works for CLIENT when allowClientEdits=false

C) BRANDING SETTINGS — ADMIN ONLY EDITS
tests/e2e/branding.spec.ts:
- OWNER/ADMIN can edit branding fields and save
- CLIENT cannot edit; UI locked; API returns 403
- Widget reflects branding (if used): brandPrimaryColor and “Powered by” toggle

D) CUSTOM DOMAIN — ADMIN ONLY + TOKEN VISIBILITY RULES
tests/e2e/customDomain.spec.ts:
- OWNER/ADMIN can set domain, see verification token
- CLIENT can view status but token is hidden/null
- Verify flow (mock DNS verifier in test mode):
  - set domain => pending
  - “verify” => verified
  - “rotate token” => token changes and status resets pending

E) NOTIFICATIONS — HOT LEAD EMAILS (MOCK SEND)
tests/e2e/notifications.spec.ts:
- OWNER/ADMIN can enable notifications and set recipients
- Trigger hot lead creation (via booking flow) => NotificationLog entry created
- Resend client mocked => “send called” asserted
- CLIENT behavior per allowClientEdits gate

F) LOGIN/INVITE FLOW (IF PRESENT IN PRODUCT)
tests/e2e/authInvite.spec.ts:
- Invite creation by OWNER/ADMIN
- Invited user accepts invite
- membership role assigned correctly
(If invites are not accessible in UI, test API path only.)

===========================================================
PHASE 5 — WIDGET + PUBLIC CHAT: TRUTH MODE + BOOKING FUNNEL
===========================================================
tests/e2e/widgetBooking.spec.ts:

Setup:
- Ensure Org A has at least 2 services with booking URLs.
- Ensure bot exists with publicKey.
- Ensure widget domain allowlist is configured for the test domain.

Test cases:
1) Widget loads
- visit /widget/<publicKey>
- chat UI renders, input enabled
- conversation starts / persisted (if designed)

2) Truth Mode grounding
- ask: “what are your hours?”
- assert response contains hours pulled from OrganizationHours
- ask something unknown
- assert response admits unknown and offers booking/lead capture fallback (no hallucinated facts)

3) Booking intent triggers FSM (deterministic)
- message: “I want to book”
- assert service selection directive renders buttons for services
- click service button => transitions to name prompt
- fill name => phone => email
- assert COMPLETE state reached
- assert booking link button appears with the correct URL for selected service
- click booking link button => BOOKING_LINK_CLICKED logged via API (PATCH /api/public/booking-click)
- assert Lead exists with serviceId attached and is idempotent:
  - re-run same completion path => no duplicate lead rows

4) Abuse/rate limits (if present)
- spam public endpoint => assert rate limit response and UI handles gracefully

===========================================================
PHASE 6 — ANALYTICS + LEADS: “PROVES IT MAKES MONEY”
===========================================================
tests/e2e/analyticsAndLeads.spec.ts:

1) Generate data:
- run widget booking flow at least twice (two unique conversations)
- create leads (hot/warm/cold if scoring logic supports)
- click booking link at least once

2) Analytics overview:
- open /app/analytics
- set date range
- assert KPIs:
  - unique conversations count uses distinct conversationId
  - funnel numbers consistent with events generated
  - clicks-by-service includes the clicked service
  - top topics reflects the chat messages

3) Lead Inbox:
- open /app/leads
- assert leads appear with correct service association
- filter by status/temperature/service and verify counts
- open lead detail drawer:
  - click-to-call/email links exist
  - scoreReasons visible
  - status update works and persists
  - notes save persists

4) CSV export:
- trigger CSV export
- assert downloaded file exists and has correct headers + includes created leadPublicId and service name

===========================================================
PHASE 7 — “CLICK EVERY CLICKABLE” STRICT MODE
===========================================================
Create tests/e2e/clickEverything.strict.spec.ts:
Goal: on each major page, click every element that is intended to be clickable without destructive action.
Rules:
- Only click elements with data-testid that match allowlist prefixes:
  nav-, link-, btn-, cta-, tab-, open-, modal-, card-
- For each click:
  - assert no page crash
  - assert either URL changed OR modal opened OR some visible state change occurred
- Record coverage report to test-results/click-coverage.json
- Fail if coverage drops (e.g., a new button without testid or not clicked)

===========================================================
PHASE 8 — SECURITY & TENANT ISOLATION GAUNTLET
===========================================================
tests/e2e/securityIsolation.spec.ts:

- Create Org A and Org B with distinct data
- For each org-scoped API route, attempt:
  - using Org A user to fetch Org B data
  - verify 404/403 and no data leak
- For RBAC:
  - CLIENT attempts admin actions (branding/domain/notifications) => must 403
  - CLIENT allowClientEdits=false attempts services/hours edits => must 403
- Ensure dev bypass cannot work in production:
  - run pnpm build && start in production mode for a small test
  - assert bypass flag ignored

===========================================================
PHASE 9 — QUALITY GATES + REPORT
===========================================================
1) Ensure all tests pass locally in CI mode:
- pnpm typecheck
- pnpm test
- pnpm test:e2e
- pnpm build

2) Generate a QA Evidence Report:
docs/QA_EVIDENCE_REPORT_FINAL.md containing:
- Summary
- Test matrix (roles x features)
- Route inventory link
- Interaction inventory link
- Coverage notes
- All command outputs
- Known limitations (if any) + follow-up items

===========================================================
IMPLEMENTATION INSTRUCTIONS (DO THIS NOW)
- Add missing data-testid attributes across:
  - public nav + footer + CTA buttons
  - app left nav
  - settings hub cards
  - services page controls, modals, inputs, error containers
  - hours page controls, inputs, row containers
  - branding/domain/notifications pages controls and error states
  - analytics page filters and key KPI elements
  - leads table rows and drawer elements
  - widget chat input, send button, service buttons, booking link button
- Add test helpers:
  - loginAs(role, org)
  - seedOrgWithDefaults(orgName)
  - seedServicesAndHours(orgId)
  - createBot(orgId) => returns publicKey
  - createConversationViaPublicChat(publicKey)
- Make tests deterministic:
  - wait for network idle or specific selectors
  - avoid arbitrary timeouts
  - use expect.poll where needed
- Do NOT rewrite existing implementation if already correct; only add minimal hooks/testids/helpers required to test everything.

START NOW: implement the entire suite as described, commit changes, and print the QA Evidence Report.