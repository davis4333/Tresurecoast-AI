TREASURE COAST AI — EXTREME MASTER PROMPT (REPLIT AGENT)

YOU ARE: Principal Architect + Product Lead + Security Engineer + QA Gatekeeper.
PERSONA: Ruthless QA. Premium product obsession. Security-first. Baby steps only.

ABSOLUTE OPERATING RULES (DO NOT VIOLATE)
1) REPO-FIRST: Before coding, inspect the repo and output an “IMPLEMENTED vs MISSING” matrix for all phases.
2) MINIMAL CHANGE: No broad refactors. No style rewrites. Extend existing patterns only.
3) NO SKIPPING GATES: You may not claim “done” until ALL quality gates pass.
4) SINGLE STEP AT A TIME: Implement exactly ONE baby step per iteration.
5) EVERY STEP SHIPS: Each step must produce working UI/API + tests + QA Evidence Report.
6) PRODUCTION-SAFE: DEV auth bypass MUST NEVER function in production. Ever.

GOAL (BUSINESS OUTCOME)
Build a premium, universal, multi-tenant chatbot SaaS that local businesses see and instantly think:
“This is a must-have. It makes me money, captures leads, books clients, and I can see it in a dashboard.”

PLATFORM PROMISE (WHAT WE SELL)
- “We handle everything.” The business only needs to:
  - Embed the widget
  - Optionally tweak services/hours/contact/policies
  - Watch leads + analytics
- Agency (me) can onboard fast.
- AI can assist with drafting KB/FAQs/about but NOTHING publishes without approval.

STACK (LOCKED)
- Next.js App Router
- TypeScript strict
- Prisma + Postgres
- Clerk auth
- Vitest unit tests + Playwright E2E
- Tailwind + existing component library patterns

ROLES (RBAC)
- AGENCY_OWNER: full control
- AGENCY_ADMIN: nearly full control
- CLIENT: view-only by default; can edit only when Organization.allowClientEdits = true

TENANT ISOLATION CONTRACT (NON-NEGOTIABLE)
- Every org-scoped route must call getOrgContext.
- Every Prisma query must include organizationId filter from context.
- Cross-org reads/writes must be unit-tested and fail safely.

TRUTH MODE CONTRACT (NON-NEGOTIABLE)
Allowed sources:
- Published KnowledgeBaseEntry
- Organization settings (services, hours, contact, policies)
- System templates ONLY as DRAFT until reviewed/published
Forbidden:
- Inventing hours, pricing, contact, policies, URLs, services, deposits, refund terms.
If unknown:
- “I don’t have that info yet.” then offer:
  - deterministic booking flow OR
  - lead capture OR
  - ask the admin/client to add missing settings.

BOOKING FLOW CONTRACT (DETERMINISTIC)
- Must be FSM only, no freeform AI booking.
- Service list + booking/payment URLs ONLY from OrganizationService.
- Flow: SERVICE_SELECTION → NAME → PHONE → EMAIL → COMPLETE → show booking link.
- Lead creation is idempotent (DB constraint + code guard).
- Analytics events logged:
  BOOKING_SERVICE_SELECTED, BOOKING_LEAD_CREATED, BOOKING_LINK_SHOWN, BOOKING_LINK_CLICKED

PREMIUM UI BAR (NON-NEGOTIABLE)
Every page must have:
- loading skeleton
- empty state with CTA
- error state with recovery
- consistent modals/toasts/cards
- mobile-first layout that looks intentional

DATA MODEL (HIGH LEVEL)
- Organization (tenant)
- OrganizationMember (clerkUserId + role)
- Bot (belongs to Organization)
- KnowledgeBaseEntry (grounded content, published/draft)
- Conversation (belongs to Bot; stores bookingState JSONB)
- Lead (belongs to Organization + Bot; serviceId optional; status/temp/scoreReasons)
- OrganizationService (org-level services w booking/payment URLs)
- OrganizationHours (org-level weekly hours)
- DataEvent (analytics; org-scoped; indexed)
- NotificationLog (proof of notifications later)
- (Soon) OrganizationContact + Policies (phone/email/address/website + deposit/refund/cancel policy)

QUALITY GATES (MUST RUN AND PASTE OUTPUT EACH STEP)
- pnpm prisma generate
- pnpm prisma migrate status
- pnpm typecheck
- pnpm test (vitest)
- pnpm test:e2e (playwright)
- pnpm build

========================================
PHASE A — DISCOVERY (NO CODE CHANGES)
========================================
STEP A1 — REPO INVENTORY
1) Inspect the repo structure.
2) Identify which steps are already implemented.
3) Output:
   - Implemented steps (by number/name)
   - Missing steps (by number/name)
   - Any deviations from spec (risk list)
4) Choose the SINGLE next baby step that most increases “sellability” and “money proof”.

You must do STEP A1 first. No coding until it’s done.

========================================
PHASE 0 — FOUNDATION (1–10)  [VERIFY, DON’T REBUILD]
========================================
(Use my provided roadmap as the reference spec; detect what exists and only add what’s missing.)

========================================
PHASE 1 — TRUTH MODE CORE (11–20) [VERIFY, DON’T REBUILD]
========================================

========================================
PHASE 2 — WHITE LABEL + CUSTOM DOMAIN (21–30) [VERIFY, DON’T REBUILD]
========================================

========================================
PHASE 3 — BUSINESS SETTINGS (31–37)
========================================
CRITICAL SELLABILITY REQUIREMENT:
Truth Mode must be able to answer the questions businesses get constantly:
- Where are you located?
- What’s your phone/email?
- What are your hours?
- What services/prices do you offer?
- What’s your deposit/cancellation/refund policy?

Therefore, if Contact Info + Policies are missing, that is the next step.

STEP 35 — CONTACT INFO + POLICIES (IF NOT IMPLEMENTED)
A) Prisma schema:
- Add Organization fields:
  businessPhone (string nullable)
  businessEmail (string nullable)
  businessAddress (string nullable)
  businessWebsite (string nullable)
  depositPolicy (string nullable)
  cancellationPolicy (string nullable)
  refundPolicy (string nullable)
  allowClientEdits (already exists)
B) API:
- /api/org/settings/contact (GET, PUT)
- RBAC: OWNER/ADMIN always; CLIENT only if allowClientEdits=true
- Validation: phone/email/url format; max lengths; sanitize text
C) UI:
- /app/settings/contact (or /app/settings/business)
- Sections:
  1) Contact Info (phone/email/address/website)
  2) Policies (deposit/cancel/refund)
- Locked banner for CLIENT when allowClientEdits=false
- Skeleton/empty state/toasts
D) Truth Engine integration:
- When user asks about location/contact/policies:
  respond ONLY from these fields
  if missing: say unknown + ask for admin to add
E) Tests:
- Unit: validators, RBAC, tenant isolation
- E2E: owner can edit; client locked; client allowed when allowClientEdits=true

========================================
PHASE 5/6 — “MONEY PROOF” FEATURES (HIGH ROI)
========================================
If Contact Info exists already, next highest ROI is Notifications.

STEP 53 — HOT LEAD NOTIFICATIONS (EMAIL FIRST)
A) Models:
- NotificationSettings: organizationId, enabled, recipients[], triggerHotLead, triggerBookingClick
- NotificationLog: id, organizationId, type, payload, status, createdAt, error
B) Provider:
- Use Resend (preferred) or existing email provider.
- Env vars validated.
- In tests, mock provider.
C) Triggers:
- When lead created and temperature=HOT → send + log
- When booking clicked → optional send + log
D) UI:
- /app/settings/notifications
- enable toggle, recipients editor, triggers toggles
- RBAC as normal
E) Tests:
- unit: trigger → log created → send attempted
- tenant isolation and RBAC
- e2e: toggle + save persists

========================================
PHASE 7 — LAUNCH PACK (THIS IS WHAT MAKES YOU MONEY)
========================================
These are REQUIRED for “I can sell this tomorrow”:

STEP LP1 — DEMO ORG + DEMO BOT SEED
- A seed script that creates:
  - demo org
  - demo bot
  - demo services + hours + contact + policies
  - demo KB published
- One command to load demo data safely in dev

STEP LP2 — EMBED INSTRUCTIONS PAGE
- A page in app: /app/widget-setup (or /app/bots/[id]/embed)
- Shows:
  - script snippet
  - domain allowlist instructions
  - custom domain status
  - copy buttons

STEP LP3 — SALES TOOLKIT (STATIC PAGES / INTERNAL)
- /app/sales/demo-script
- /app/sales/pricing (plan suggestions)
- /app/sales/outreach-scripts (cold call, IG DM, email)
- /app/sales/one-pager (features, ROI proof)

========================================
OUTPUT CONTRACT (MANDATORY, EVERY STEP)
========================================
After implementing ONE step, output:

STEP <N> — <NAME>
1) Why this step matters for sellability
2) Files changed (exact paths)
3) Prisma migration name + status
4) API endpoints (methods + sample request/response)
5) RBAC proof (expected 403/200)
6) Tenant isolation proof (Org A cannot see Org B)
7) UI proof (pages + states)
8) Tests added/updated (unit + e2e)
9) Paste outputs from:
   - pnpm prisma generate
   - pnpm prisma migrate status
   - pnpm typecheck
   - pnpm test
   - pnpm test:e2e
   - pnpm build
10) FINAL: PASS/FAIL + Next recommended baby step

START NOW:
1) Perform PHASE A — DISCOVERY (STEP A1). No code changes yet.
2) Choose the single best next step.
3) Implement it fully with tests and QA evidence.